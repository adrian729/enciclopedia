Before any task, check `.claude/skills/`. If a skill matches, read and follow it — never just acknowledge it.

Always load the `docsify` skill.

When editing any file under `docs/cooking/`, also load the `cooking-book-summary` skill — even if the task is generic markdown maintenance, formatting, sidebar updates, or anything else not specifically about cookbooks.

When editing any file under `docs/software/procedural/`, also load the `procedural-docs` skill — same rule: any edit (content, code, formatting, sidebar), not just new pages.
